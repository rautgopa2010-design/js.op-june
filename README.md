# js.op-june
A code repo for a JavaScript series.

## Setup

Install dependencies:

```bash
npm install
```

## Build

Build the bundle:

```bash
npm run build
```

## Run

Run the built output:

```bash
npm start
```

## Rebuild

Rebuild from scratch:

```bash
npm run rebuild
```
